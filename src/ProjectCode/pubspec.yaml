name: cortal
description: Complaint Management Portal.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  #Firebase
  firebase_auth: ^3.0.1
  firebase_core: ^1.4.0
  cloud_firestore: ^2.4.0

  #Backend
  http: ^0.13.3 ##HTTP

  flutter_spinkit: ^5.0.0 ## Spinkit for loading screen
  google_fonts: ^2.1.0 ## Fonts

  awesome_dialog: ^2.1.0 ## Error dialog

  shared_preferences: ^2.0.6 #Shared Preferences

  uuid: ^3.0.4 #Random ID Generator

  syncfusion_flutter_charts: ^19.2.55 #Chart

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/registration/
